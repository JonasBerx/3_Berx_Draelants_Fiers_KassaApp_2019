package model.basket.state;

import model.basket.Basket;

/**
 * @author Pieter Fiers
 * States
 * */

public class PayedState extends BasketState {
    public PayedState(Basket basket) {
        super(basket);
    }
}
